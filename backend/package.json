{
  "name": "backend",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "nodemon --watch 'src/**/*.ts' --exec ts-node src/server.ts",
    "prod": "nodemon --watch 'src/**/*.ts' --exec ts-node src/server.ts",
    "pull:dev": "dotenv -e .env.development -- npx prisma db pull",
    "pull:prod": "dotenv -e .env.production -- npx prisma db pull",
    "push:dev": "dotenv -e .env.development -- npx prisma db push",
    "push:prod": "dotenv -e .env.production -- npx prisma db push",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^5.11.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "dotenv-cli": "^7.4.1",
    "express": "^4.19.1",
    "node": "^21.7.1",
    "prisma": "^5.11.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/dotenv": "^8.2.0",
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.30",
    "nodemon": "^3.1.0",
    "ts-node": "^10.9.2"
  }
}
